
= render "templates/header"
.categories_header
  - if @category.blank?
    %h2 カテゴリー目次
  - else
    %h2=@category.name
  .categories_header-category
    - @categories.each_with_index do |category,num|
      = link_to category.name ,category_path(category.id) ,data:{category_index:num}

      